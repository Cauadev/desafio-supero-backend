server:
  port: 8083
  
spring:  application:    name: desafio-supero-backend
      datasource:    url: jdbc:mysql://localhost:3306/supero?useTimeZone=trues&serverTimeZone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 
    
  jpa:    hibernate:      ddl-auto: update
    properties:      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect